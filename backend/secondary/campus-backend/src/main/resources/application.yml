spring:
  datasource:
    url: ${DATABASE_URL:jdbc:postgresql://kindlyfr:0KRCRal4YdOE3YKC8fOlv8xEi2NtdadY@dpg-d6eo0hrh46gs73e6n84g-a.oregon-postgres.render.com:5432/cdb_oter?sslmode=require}
    username: ${DATABASE_USERNAME:kindlyfr}
    password: ${DATABASE_PASSWORD:0KRCRal4YdOE3YKC8fOlv8xEi2NtdadY}
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 5
      connection-timeout: 30000

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true

server:
  port: ${PORT:8080}

  school21:
    api:
      base-url: https://platform.21-school.ru/services/21-school/api
      # !!! ВАЖНО: Никогда не храните ключи в коде. Используйте переменные окружения.
      api-key: ${SCHOOL21_API_KEY:your-default-key-for-dev}
    # Доп. настройки, если нужны
    client:
      connect-timeout: 5000
      read-timeout: 10000




