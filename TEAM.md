# TEAM.md 

## Состав команды и распределение ответственности

### Команда
- Team TL:theomorn

### Лидер
- Роман (@theomorn)

### Участники
- Тимур (@kindlyfr) — арифметика (+, −), сравнения (3 функции)
- Настя (@roselint) — арифметика (×, ÷), сравнения (3 функции)
- Фёдор (@groupmol) — конвертация int, математика (truncate, negate)
- Роман (@theomorn)👑 — конвертация float, математика (round, floor)



## 📌 Структура проекта

| Модуль               | Функциональность                     | Авторы                     |
|----------------------|--------------------------------------|----------------------------|
| `s21_arithmetic.c`   | Арифметические операции (+, −, ×, ÷) | Тимур (@kindlyfr), Настя (@roselint) |
| `s21_comparison.c`   | Операции сравнения (6 функций)       | Тимур (@kindlyfr), Настя (@roselint) |
| `s21_convert.c`      | Конвертация (int/float ↔ decimal)    | Фёдор (@groupmol), Роман (@theomorn) |
| `s21_math.c`         | Математические функции (4 функции)   | Фёдор (@groupmol), Роман (@theomorn) |
| `s21_helpers.c`      | Вспомогательные утилиты              | Все 4                      |
| `tests/test.c`       | Тесты                                | Все 4                      |
| `s21_decimal.h`      | Публичный интерфейс                  | Все 4                      |
| `s21_decimal.с`      | #include других .c файлов            | Все 4                      |
| `Makefile`           | Сборка                               | Все 4                      |

---
Все .c файлы компилируются в объектники и архивируются в одну статическую библиотеку s21_decimal.a

### Makefile - цели
- all
- clean
- test
- s21_decimal.a
- gcov_report
- format - проверка форматирования
- cppcheck - статический анализ
- valgrind - запускает тесты под valgrind

```bash
make test
```

## 👤 Распределение функций по авторам

### Тимур (@kindlyfr)
| Функция            | Модуль             | Статус    |
|--------------------|--------------------|-----------|
| `s21_add`          | `s21_arithmetic.c` | 🟢 Готово |
| `s21_sub`          | `s21_arithmetic.c` | 🟢 Готово |
| `s21_is_less`      | `s21_comparison.c` | 🟢 Готово |
| `s21_is_equal`     | `s21_comparison.c` | 🟢 Готово |
| `s21_is_not_equal` | `s21_comparison.c` | 🟢 Готово |

### Настя (@roselint)
| Функция                     | Модуль             | Статус   |
|-----------------------------|--------------------|----------|
| `s21_mul`                   | `s21_arithmetic.c` | 🟡 В работе |
| `s21_div`                   | `s21_arithmetic.c` | 🟡 В работе |
| `s21_is_greater`            | `s21_comparison.c` | 🟡 В работе |
| `s21_is_less_or_equal`      | `s21_comparison.c` | 🟡 В работе |
| `s21_is_greater_or_equal`   | `s21_comparison.c` | 🟡 В работе |

### Фёдор (@groupmol)
| Функция                     | Модуль          | Статус   |
|-----------------------------|-----------------|----------|
| `s21_from_int_to_decimal`   | `s21_convert.c` | 🟢 Готово |
| `s21_from_decimal_to_int`   | `s21_convert.c` | 🟢 Готово |
| `s21_truncate`              | `s21_math.c`    | 🟢 Готово |
| `s21_negate`                | `s21_math.c`    | 🟢 Готово |

### Роман (@theomorn)
| Функция                     | Модуль          | Статус   |
|-----------------------------|-----------------|----------|
| `s21_from_float_to_decimal` | `s21_convert.c` | 🟡 В работе |
| `s21_from_decimal_to_float` | `s21_convert.c` | 🟡 В работе |
| `s21_round`                 | `s21_math.c`    | 🟡 В работе |
| `s21_floor`                 | `s21_math.c`    | 🟡 В работе |

### `s21_helpers.c` (все 4)
| Утилита                     | Ответственный       | Примечание                     |
|-----------------------------|---------------------|--------------------------------|
| Битовые операции (scale/sign) | Фёдор (@groupmol)   | Базовые операции над `bits[3]` |
| Валидация структуры         | Роман (@theomorn)   | `s21_is_valid_decimal()`       |
| Работа с мантиссой (96 бит) | Тимур (@kindlyfr)   | Сложение/вычитание мантиссы    |
| Округление (банковское)     | Настя (@roselint)   | Для `mul`, `div`, `from_float` |

###  статусы 🟢/🟡/🔴
-  🟢 Готово
-  🟡 В работе
-  🔴 Проблемы

---

## 📜 Правила командной работы

1. **Каждый модуль — один файл**  
   Не разделять логически связанные функции по разным файлам (например, все операции сравнения — в `s21_comparison.c`).

2. **Вспомогательные функции — `static`**  
   Все утилиты внутри модуля объявляются как `static`. Исключение — функции из `s21_helpers.c`, которые используются в нескольких модулях.

3. **Изменения в чужом коде**  
   - Требуют согласования через Merge Request
   - Обязательное ревью автора оригинального кода
   - Комментарий `/* [Имя]: правка от 2026-02-13 — причина */` при изменении чужой функции

4. **Тестирование**  
   - Автор функции пишет юнит-тесты для неё
   - Интеграционные тесты (например, `add` → `to_int`) — ответственность обоих авторов затронутых модулей

5. **Ветки в Git**  
   ```text
   tree/develop
   tree/all
   tree/kindlyfr
   tree/roselint
   tree/groupmol
   tree/theomorn

---

## 🤝 Правила совместной работы над файлами

###  Пример оформления `s21_arithmetic.c` (Тимур + Настя) 
```c
// s21_arithmetic.c
/* ============================================================
 * Модуль: Арифметические операции (+, −, ×, ÷)
 * Ответственные: 
 *   - Тимур (@kindlyfr): s21_add, s21_sub
 *   - Настя (@roselint): s21_mul, s21_div
 * ============================================================ */

/* Автор: Тимур (@kindlyfr) */
int s21_add(s21_decimal value_1, s21_decimal value_2, s21_decimal *result) { ... }

/* Автор: Тимур (@kindlyfr) */
int s21_sub(s21_decimal value_1, s21_decimal value_2, s21_decimal *result) { ... }

/* ---------------------------------------------------------- */

/* Автор: Настя (@roselint) */
int s21_mul(s21_decimal value_1, s21_decimal value_2, s21_decimal *result) { ... }

/* Автор: Настя (@roselint) */
int s21_div(s21_decimal value_1, s21_decimal value_2, s21_decimal *result) { ... }
```
---


### Совместная работа над модулями

#### `s21_comparison.c` (Тимур + Настя)
- Каждый автор отвечает за свои функции сравнения
- Общие вспомогательные функции (например, `s21_normalize_for_comparison`) — согласование через ревью

#### `s21_math.c` (Фёдор + Роман)
- Фёдор: `truncate`, `negate`
- Роман: `round`, `floor`
- Общие утилиты округления — согласование обоих авторов

---

##  🐱⎇ Git-процесс команды: all → личная ветка → all → develop

### Ветки
- `master` — не трогаем
- `develop` — финальная ветка проекта
- `all` — общая интеграционная ветка
- `<me>` — личная ветка разработчика (`theomorn`, `roselint`, `groupmol`, `kindlyfr`)

---

### 1) Подтянуть изменения из `all` в свою ветку

### Вариант A (рекомендуется): rebase
```bash
git checkout <me>
git fetch origin
git rebase origin/all

Если конфликт:

# исправить конфликт в файлах
git add <file>
git rebase --continue

Отменить rebase:

git rebase --abort

Если ветка уже была запушена ранее:

git push --force-with-lease origin <me>

Вариант B: merge

git checkout <me>
git fetch origin
git merge origin/all

Если конфликт:

# исправить конфликт
git add <file>
git commit

2) Передать свои изменения в all
Вариант A (рекомендуется): через Merge Request (MR)

    Коммит:

git checkout <me>
git add .
git commit -m "feat: <short description>"

    Push своей ветки:

git push -u origin <me>

    Создать MR:

    Source branch: <me>

    Target branch: all

После мержа MR код появляется в all.
Вариант B (по договорённости): напрямую в all

git checkout all
git pull origin all
git merge <me>
git push origin all

3) Как другим участникам получить изменения из all

Через rebase:

git checkout <their_branch>
git fetch origin
git rebase origin/all

Или через merge:

git checkout <their_branch>
git fetch origin
git merge origin/all

4) Краткая шпаргалка

Обновить свою ветку из all:

git checkout <me>
git fetch origin
git rebase origin/all

Передать изменения в all:

git push origin <me>
# MR <me> -> all

5) Кратко: что такое fetch / merge / rebase
git fetch

Скачивает изменения с удалённого репозитория.
Не изменяет текущую ветку и рабочие файлы.
Обновляет ссылки origin/*.
git merge

Сливает одну ветку в другую.
Сохраняет историю обеих веток.
Может создать merge-коммит.
git rebase

Переписывает историю текущей ветки,
перенося её коммиты поверх другой ветки.
История становится линейной.

Правила:

    rebase делать только в личных ветках

    не делать rebase в all и develop
    
```


# Как исправить изменения в Git (шпаргалка)

## 0) Перед любыми откатами

Посмотреть, что происходит:
```bash

git status
git log --oneline --decorate -n 15

1) Косяк в файлах, но коммита ещё нет
A) Отменить изменения в конкретном файле (вернуть как было в HEAD)

git restore <file>

B) Отменить все изменения в рабочей директории

git restore .

C) Убрать файл из staged (после git add), но оставить изменения

git restore --staged <file>

2) Коммит сделал, но НЕ пушил
A) Оставить изменения, но убрать последний коммит (самое частое)

git reset --soft HEAD~1

После этого можно поправить файлы и сделать новый коммит.
B) Убрать последний коммит и изменения тоже (жёстко)

git reset --hard HEAD~1

C) Изменить сообщение/содержимое последнего коммита

# поправь файлы, если нужно
git add .
git commit --amend

3) Коммит уже запушен в ЛИЧНУЮ ветку (<me>)
Безопасный вариант (рекомендуется): сделать "откатный" коммит

Не переписывает историю:

git revert <hash>
git push origin <me>

Если нужно переписать историю (только если ветка точно твоя и никто её не использует)

git reset --hard HEAD~1
git push --force-with-lease origin <me>

Правило:

    --force-with-lease безопаснее, чем --force

4) Косяк попал в all (или develop)

В общих ветках историю НЕ переписываем. Только revert.
Откат одного коммита в all

git checkout all
git pull origin all
git revert <hash>
git push origin all

Откат merge-коммита (MR/merge)

Нужно указать "родителя" (обычно 1):

git checkout all
git pull origin all
git revert -m 1 <merge_hash>
git push origin all

5) Косяк при rebase (конфликты или всё поехало)
A) Отменить rebase целиком

git rebase --abort

B) Продолжить после исправления конфликтов

# исправь файлы
git add <file>
git rebase --continue

6) Неправильно смёржил ветку / конфликтный merge
A) Если merge ещё НЕ закоммичен (ты внутри merge)

git merge --abort

B) Если merge-коммит уже создан, но не пушен

git reset --hard HEAD~1

C) Если merge уже пушен в общую ветку (all)

Откатываем revert ом:

git checkout all
git pull origin all
git revert -m 1 <merge_hash>
git push origin all

7) Случайно удалил/потерял коммит

Найти потерянный коммит:

git reflog

Вернуться:

git reset --hard <hash_из_reflog>

8) Быстрые ориентиры: что безопасно

    В личной ветке (<me>): можно reset и push --force-with-lease, если никто не использует

    В all и develop: только revert (без force)

